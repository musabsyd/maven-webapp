pipeline {
    agent any
    
    tools{
        maven "Maven"
    }

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/musabsyd/mavenweb-app.git'
            }
        }
        stage('Maven Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Docker Image') {
            steps {
                sh 'docker build -t musabsyed/mavenwebapp .'
            }
        }
        stage('k8s deployment') {
            steps {
                sh 'kubectl apply -f k8s-deploy.yml'
            }
        }
    }
}


