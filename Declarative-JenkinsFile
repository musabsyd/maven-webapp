pipeline {
    agent any
    
    tools{
        maven "Maven"
    }

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/musabsyd/maven-webapp.git'
            }
        }
         stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}

